var config = {
  'server' : {
    'host' : 'api.ticketless.dev',
    'port' : 8887
  },
  // Note: Database object is passed directly to .connect(), in database.js
  'database' : {
    'host'     : '127.0.0.1',
    'user'     : 'ticketless',
    'password' : 'bojihunter',
    'database' : 'ticketless'
  }
}

module.exports = config;